name: Soroban Guard Basic
on: [pull_request]
jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: your-org/soroban-guard-action@v1
        with:
          path: contracts/
